{
    "$schema": "https://turbo.build/schema.json",
    "pipeline": {
        "generate": {
            "inputs": [
                "input/**"
            ],
            "outputs": [
                "output/**"
            ]
        },
        "start": {
            "dependsOn": [
                "generate"
            ]
        }
    }
}